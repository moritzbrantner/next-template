# Showcase Tests

These tests enforce showcase-owned contract behavior at the app-pack seam:

- manifest route and feature-gating behavior
- message namespace completeness
- content-root validity

Foundation-wide architectural checks still live under `tests/unit`.
